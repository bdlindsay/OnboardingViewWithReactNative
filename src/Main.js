import React from 'react'
import { SafeAreaView, Text } from 'react-native'

export default () => (
	<SafeAreaView style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
		<Text style={{ fontWeight: '700', fontSize: 24 }}>Main</Text>
	</SafeAreaView>
)
